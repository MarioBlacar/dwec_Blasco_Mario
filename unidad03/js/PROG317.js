let nombres = [
  {
    name: "Victor",
    surname: "González",
  },
  {
    name: "Pepe",
    surname: "Pérez",
  },
];

function generateUsername(arr) {
  return arr.map((item) => item ** pow);
}

console.log(generateUsername(nombres));
